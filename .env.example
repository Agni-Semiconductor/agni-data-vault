# Client (Vite, exposed to browser — never put secrets here)
VITE_SUPABASE_URL=
# Client (Vite, exposed to browser — never put secrets here)
VITE_SUPABASE_ANON_KEY=
# Server (Vercel function only)
SUPABASE_URL=
# Server (Vercel function only)
SUPABASE_SERVICE_ROLE_KEY=
# Server (Vercel function only)
VAULT_API_KEY=
