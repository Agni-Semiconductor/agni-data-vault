import crypto from 'node:crypto';
export function requireAuth(req, res) { if (req.method?.toUpperCase() === 'OPTIONS') return true; const expected = process.env.VAULT_API_KEY; if (!expected) { res.status(500).json({ error: { code: 'server_misconfigured', message: 'VAULT_API_KEY is not set on the server' } }); return false; } const header = req.headers.authorization || ''; const token = header.startsWith('Bearer ') ? header.slice(7).trim() : ''; const a = Buffer.from(token); const b = Buffer.from(expected); const ok = a.length === b.length && crypto.timingSafeEqual(a, b); if (!ok) { res.status(401).json({ error: { code: 'unauthorized', message: 'Provide Authorization: Bearer <VAULT_API_KEY>' } }); return false; } return true; }
