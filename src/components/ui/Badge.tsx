import clsx from 'clsx'
export function Badge({ children, tone = 'gray', className }: { children: React.ReactNode; tone?: 'gray'|'green'|'amber'|'red'|'blue'; className?: string }) { return <span className={clsx('inline-flex rounded-full border px-2 py-0.5 text-xs font-semibold', { gray: 'border-agni-slate bg-white text-agni-slate', green: 'border-success bg-white text-success', amber: 'border-agni-gold bg-agni-gold text-agni-ink', red: 'border-danger bg-white text-danger', blue: 'border-border-subtle bg-agni-orange-tint text-agni-orange' }[tone], className)}>{children}</span> }
