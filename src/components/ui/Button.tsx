import { forwardRef, type ButtonHTMLAttributes } from 'react'
import clsx from 'clsx'
import { Spinner } from './Spinner'
export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { variant?: 'primary'|'secondary'|'ghost'|'danger'; size?: 'sm'|'md'; loading?: boolean }
export const Button = forwardRef<HTMLButtonElement, ButtonProps>(function Button({ className, variant = 'primary', size = 'md', loading = false, disabled, children, ...props }, ref) { return <button ref={ref} disabled={disabled || loading} className={clsx('inline-flex items-center justify-center gap-2 rounded-md font-medium disabled:cursor-not-allowed disabled:opacity-50', size === 'sm' ? 'h-8 px-3 text-xs' : 'h-9 px-4 text-sm', { primary: 'bg-agni-orange text-white hover:bg-agni-orange-hover', secondary: 'border border-agni-orange bg-white text-agni-orange hover:bg-agni-orange-tint', ghost: 'text-agni-ink hover:bg-surface-2', danger: 'border border-[#B3261E] bg-white text-[#B3261E] hover:bg-[#fff1f0]' }[variant], className)} {...props}>{loading && <Spinner size="sm" />}{children}</button> })
