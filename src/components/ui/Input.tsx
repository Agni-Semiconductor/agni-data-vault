import { forwardRef, type InputHTMLAttributes } from 'react'
import clsx from 'clsx'
export interface InputProps extends InputHTMLAttributes<HTMLInputElement> { label?: string; error?: string; hint?: string; unit?: string }
export const Input = forwardRef<HTMLInputElement, InputProps>(function Input({ label, error, hint, unit, className, id, ...props }, ref) { const inputId = id ?? label?.toLowerCase().replace(/\s+/g, '-'); return <label className="block text-sm font-medium text-gray-700" htmlFor={inputId}>{label && <span className="mb-1 block">{label}</span>}<span className="relative block"><input ref={ref} id={inputId} className={clsx('block w-full rounded-md border px-3 py-2 text-sm outline-none focus:ring-2 focus:ring-blue-500', error ? 'border-red-500' : 'border-gray-300', unit && 'pr-12', className)} {...props} />{unit && <span className="pointer-events-none absolute inset-y-0 right-3 flex items-center text-xs text-gray-500">{unit}</span>}</span>{error ? <span className="mt-1 block text-xs text-red-600">{error}</span> : hint && <span className="mt-1 block text-xs text-gray-500">{hint}</span>}</label> })
