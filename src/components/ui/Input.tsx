import { forwardRef, type InputHTMLAttributes } from 'react'
import clsx from 'clsx'
export interface InputProps extends InputHTMLAttributes<HTMLInputElement> { label?: string; error?: string; hint?: string; unit?: string }
export const Input = forwardRef<HTMLInputElement, InputProps>(function Input({ label, error, hint, unit, className, id, ...props }, ref) { const inputId = id ?? label?.toLowerCase().replace(/\s+/g, '-'); return <label className="block text-xs font-medium text-agni-ink" htmlFor={inputId}>{label && <span className="mb-1 block">{label}</span>}<span className="relative block"><input ref={ref} id={inputId} className={clsx('block w-full rounded-md border border-border-subtle bg-white px-3 py-2 text-sm outline-none focus:border-agni-orange', error && 'border-[#B3261E]', unit && 'pr-12', className)} {...props} />{unit && <span className="pointer-events-none absolute inset-y-0 right-3 flex items-center font-mono text-xs text-agni-slate">{unit}</span>}</span>{error ? <span className="mt-1 block text-xs text-[#B3261E]">{error}</span> : hint && <span className="mt-1 block text-xs text-agni-slate">{hint}</span>}</label> })
