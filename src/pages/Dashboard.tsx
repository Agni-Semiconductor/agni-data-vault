import { useState } from 'react'
import { Link } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import { Button, Spinner } from '../components/ui'
import { getStats } from '../lib/api'
import { CreateSampleModal } from './SamplesList'
export default function Dashboard() { const [createOpen,setCreateOpen]=useState(false); const {data,error,isLoading}=useQuery({queryKey:['stats'],queryFn:getStats}); if(isLoading)return <Spinner/>; if(error)return <p className="text-red-600">{error.message}</p>; if(!data)return null; const max=Math.max(1,...Object.values(data.by_kind)); return <div className="space-y-6"><div className="flex justify-between"><h1 className="text-xl font-semibold">Dashboard</h1><Button onClick={()=>setCreateOpen(true)}>New sample</Button></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Samples',data.samples],['Measurements',data.measurements],['Files',data.files],['Storage',`${(data.bytes/1048576).toFixed(1)} MB`]].map(([label,value])=><div key={String(label)} className="rounded border p-4"><p>{label}</p><p className="text-2xl font-semibold">{value}</p></div>)}</div><div className="grid gap-6 lg:grid-cols-2"><section><h2 className="font-semibold">By kind</h2>{Object.entries(data.by_kind).map(([kind,count])=><div key={kind}><div className="flex justify-between"><span>{kind}</span><span>{count}</span></div><div className="h-2 bg-gray-100"><div className="h-2 bg-blue-600" style={{width:`${count/max*100}%`}}/></div></div>)}</section><section><h2 className="font-semibold">Recent measurements</h2><table className="w-full text-sm"><thead><tr><th>Date</th><th>Sample key</th><th>Kind</th><th/></tr></thead><tbody>{data.recent.map(row=><tr key={row.id}><td>{row.measured_on}</td><td><Link to={`/samples/${row.sample_key}`}>{row.sample_key}</Link></td><td>{row.kind}</td><td><Link to={`/measurements/${row.id}`}>Open</Link></td></tr>)}</tbody></table></section></div><CreateSampleModal open={createOpen} onClose={()=>setCreateOpen(false)}/></div> }
